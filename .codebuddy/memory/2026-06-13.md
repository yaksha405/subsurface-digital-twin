# Daily Log

## 2026-06-13

### 第一轮：炼油化工管网连通性修复
- **用户反馈**: H-101(加热炉)和C-101(蒸馏塔)相互连接，但与换热器组(E-101~E-104)完全断开，机器人无法进入
- **根因**: refineryDataGenerator.ts 第126行的连接管坐标不匹配 — y=-8 但换热器出口在 y=-10, z=0 但出口在 z=-12/-4/4/12, 无集合总管汇集
- **修复** (`refineryDataGenerator.ts` +20/-1):
  1. 新增换热器入口集合总管 (x=-30, y=-8.5, z=-12→12) 连接所有换热器入口
  2. 新增换热器出口集合总管 (x=-6, y=-10, z=-12→12) 汇集所有换热器出口
  3. 新增立管 (vPath) 从出口集合管上升到加热炉入口标高
  4. 新增加热炉入口分配管连接到辐射段U型管起点
  5. 新增机器人检修入口总管从场景外部(x=-36)接入
  6. 新增C-101塔底回流闭环: 塔底→回流总管→换热器壳程
- **管网回路**: 机器人入口→入口总管→4台换热器→出口总管→立管→H-101→C-101→回流总管→换热器壳程
- **Push**: commit `48beaa3`, Vercel 部署中

### 第二轮：三大工业场景管网全面连通性修复
- **用户反馈**: 不止炼油化工，所有场景都有大量互不连通的管道
- **诊断方法**: 编写图论分析脚本(并查集+路径采样点匹配,TOL=1.2), 系统检查所有管道端点和中间点
- **发现**:
  - 油气管线: 24根中14根断开(分配管D1/D2/D3起点在干线范围外, 干线跟汇管采样点对不上)
  - 核反应堆: 34根中29根断开(4环路间无公共RPV, SG进出口不匹配, 主蒸汽/给水完全漂浮)
  - 炼油化工: 56根中18根断开(H-101辐射管无集合管, C-101内部通道全漂浮)
- **修复** (3文件 +80/-24):
  - **pipelineDataGenerator.ts**: 干线起点从汇管中点(x=0,采样对不上)改为W4立管位置(x=7); 分配管X从[-10,5,20,35]→[14,22,30,38]; 计量支线X从[25,32,39]→[20,28,36]
  - **nuclearDataGenerator.ts**: 新增SG传热管×4(闭合一回路环路); SG汽侧出口×4+给水→SG冷侧×4; RPV热腿/冷腿分配环管(半径3.5圆环); RPV堆芯下降段; 修复SG排污管坐标
  - **refineryDataGenerator.ts**: H-101辐射入口/出口集合管(东南西北4×2条)+入口汇集母管+辐射→对流跨越+对流层间跨越+对流出口弯管; C-101进料分配器+进料竖管+精馏顶→降液管+汽提→降液管+塔顶内连接+柴油/煤油抽出内管+汽提底→塔底管
- **验证**: 三场景均 1个连通分量, 0根断开 ✅; tsc 零错误
- **Push**: commit `a7ae741`

### 用户要求: 连通性检查写入永久记忆
- 用户明确要求: 图论连通性检查必须作为所有场景的强制规则，写入 MEMORY.md
- 适用: 管道、裂缝、河流、隧道等所有线状要素场景
- 已更新 MEMORY.md 的"强制规则"章节

### 第三轮：新增模拟数据五·地下暗流场景
- **需求**: 地质勘探中的地下流体通道（岩溶暗河/油气运移），管径变化（狭窄→开阔），蛇形+猪形机器人协作
- **数据生成器**: `undergroundDataGenerator.ts` (新建, 380行)
  - 5种通道类型: trunk(主干暗河2-5.5m) / tributary(支流0.8-2.5m) / constriction(瓶颈0.3-0.8m) / chamber(溶洞4-8m) / blind_end(盲端1.5-4m)
  - 21根通道: 3主干+3溶洞+3瓶颈+6支流+3合流+3盲端
  - 物理参数: 地温梯度25°C/km, 渗透率立方定律, 雷诺数, H₂S最高800ppm
  - 参考文献: KarstConduitCatalogue(ESSD 2025), Jiang et al.2024, Worthington 2015
- **图论连通性验证**: 21根通道, 1个连通分量, 0断开 ✅ (首次检查发现溶洞A/B起点偏移主干3-4m导致3分量, 修复起点精确对齐后全部连通)
- **集成**: 14文件更新 — types/robotDataGenerator/alertDataGenerator/ScenarioSelector/FractureNetwork/RockMass/RobotMarkers/aiApi/mockAI/sceneMeasureConfig/fractureApi/llmTools/useRobots
- **机器人**: 蛇形(55%)+履带(30%)+蛛型(15%)混合编队, 14种任务
- **Push**: commit `09db0e5`

### 第四轮：地下暗流视觉重构 — 去管道化 + 新增浮走机器人
- **用户反馈**: 地下暗流像金属管道(巨粗、金属质感、岩体没覆盖到)
- **网络调研**: 石油不是地下湖泊而是岩石微孔隙(1500-7500m深)，不适合机器人穿行；地下水岩溶暗河(10-800m)才是机器人可穿行空间
- **视觉重构**:
  - `FractureNetwork.tsx`: 新增 `UndergroundChannelMesh` — 水流质感(非金属 meshPhysicalMaterial: metalness=0, transmission=0.3, ior=1.33, clearcoat), 蓝/琥珀色流体着色, 无球形接头
  - `RockMass.tsx`: 地下场景岩体尺寸 130×90×110m, 中心下沉 y=-20, 不透明度0.72 完全包裹通道
  - `undergroundDataGenerator.ts`: 管径大幅缩小 trunk 2.0-5.5m→0.35-0.55m, chamber 4-8m→0.5-0.8m; 去油气(流体改为水/岩溶水/承压水); 通道名 UC→UR
  - 传感器/告警/测量/AI prompt 全部去油气改为水焦点
- **新增浮走式机器人** (`floatwalker`):
  - `RobotModel` 新增 `'floatwalker'` 类型
  - `RobotMarkers.tsx`: 新增 `FloatWalkerMarker` — 半透明伞盖(脉动收缩)+6条触须(随水流飘摆)+整体上下漂浮+软发光
  - 地下暗流场景权重: 浮走50% + 蛇形30% + 履带12% + 蛛型8%
  - 全链路标签更新: types/index.ts, RobotDetailDialog, RobotFleet(MODEL_OPTIONS), pdfExport(补spider+floatwalker), aiApi, ScenarioSelector, robotDataGenerator

### 第五轮：地下暗流三项修复
- **着色问题**: UndergroundChannelMesh 之前用固定 flat color(#1A5588)，不像其他场景有传感器渐变。改为 TubeGeometry 顶点着色 — 沿路径插值节点渗透率数据, 用 valueToColor() 做 蓝→绿→黄→红 渐变
- **标题问题**: ChatPanel.tsx 缺少 `underground` 分支 → 回退到"地质裂缝分析AI助手"。已补 `if (scenario === 'underground') return '地下暗流探测AI助手'`
- **机器人**: 用户要求地下场景只用章鱼。MODEL_WEIGHTS_UNDERGROUND 改为 floatwalker:100（去掉蛇形/履带/蛛型）。AI prompt/欢迎语/mockAI 全部去掉其他机型

### 第六轮：建立场景渲染标准色系
- **用户需求**: 各场景渲染颜色应有统一标准，不能随意
- **调研**: ANSI/ASME A13.1(管道色)、DZ/T 0179(国标地质色)、IS 2379(管道详细)、科学可视化 turbo colormap
- **新建 `src/lib/sceneColors.ts`** — 标准色系单文件:
  - `STATUS`: danger/warning/caution/safe (红绿灯)
  - `INTERACTION`: selected/hover/highlight (黄色系)
  - `ROBOT_STATUS`: 5种状态色
  - `PIPE_IDENTITY`: ANSI标准管道色 — water绿/flammable棕/toxic橙/fire红/compressed蓝/steam银灰/stainless青/hotAlloy琥珀
  - `GEO_IDENTITY`: 国标地质色 — sedimentary黄棕/igneous红粉/metamorphic绿紫/groundwater蓝/fault红
  - `SCENARIO_BASE_COLOR`: 各场景默认正常色映射
  - `valueToColor()`: 统一 turbo colormap 蓝→绿→黄→红
  - `ENTRANCE`: 入口标记色
- **已应用**: RobotMarkers(ROBOT_STATUS)、FractureNetwork(getPipeColor+getFractureNodeMarkerColor 用 STATUS/SCENARIO_BASE_COLOR)、RobotDetailDialog、RobotFleet

### 第七轮：五场景色彩标准全量合规 + 地下暗流远视角变黑修复
- **用户反馈**: (1) 是否五个模拟场景都按标准着色了？(2) 地下暗流拉远视角后通道变黑看不见
- **根因分析**:
  - 地下暗流通道用了 `meshPhysicalMaterial` 的 `transmission=0.3` — 无环境贴图时光线穿透后无物可折射→通道变黑
  - 通道默认 emissive=#000000, intensity=0 — 完全依赖场景光照, 远视角下被半透明暗色岩体吞没
  - 岩体外壳 opacity=0.72 过高
  - RockMass/ReactorContainment/入口标记大量硬编码色值未引用标准常量
- **修复**:
  1. `FractureNetwork.tsx` — UndergroundChannelMesh: meshPhysicalMaterial→meshStandardMaterial(去transmission/ior/clearcoat), 基础emissive=GEO_IDENTITY.waterGlow intensity=0.22, renderOrder=2; PipeMesh/FractureSurface/入口标记全部改用 INTERACTION/GEO_IDENTITY/ENTRANCE/NUCLEAR_IDENTITY 常量
  2. `RockMass.tsx` — 全部硬编码色值→GEO_IDENTITY常量; 地下外壳opacity 0.72→0.5, 内壳0.55→0.38
  3. `ReactorContainment.tsx` — 全部硬编码色值→NUCLEAR_IDENTITY常量
  4. 新建 `docs/COLOR_STANDARD.md` — 强制规范文档, 含五大场景映射表+四大色彩体系+地下暗流可见性规则+编码规范
- **tsc**: 零错误

### 第八轮：数据导出 / 交付中心 (Export Hub) — 四大工业标准格式
- **用户需求**: 数据可导出到 Trimble/CAD/测绘等工业软件，建立"双轨制"商业定位(SaaS看板+工业导出)
- **新建四大导出模块**:
  1. `src/lib/exportLAS.ts` — LAS 1.4 点云导出 (ASPRS标准, 兼容Trimble/AutoCAD/ArcGIS/CloudCompare)
     - Point Data Record Format 2 (含RGB), 26字节/点
     - 点来源: 体素点云(~60K点) + 裂缝节点 + 裂缝路径 + 在线机器人
     - ASPRS分类码: 1=未分类扫描点, 2=主裂缝, 63=路径线, 64=机器人
     - RGB着色: CH4浓度 turbo colormap 映射
  2. `src/lib/exportOBJ.ts` — OBJ+MTL 网格导出 (Wavefront标准, 兼容Blender/3dsMax/3D打印)
     - 裂缝管道→TubeGeometry(CatmullRom曲线), 体素→简化三角面片, 机器人→OctahedronGeometry
     - MTL材质库: 主缝黄/分支橙/岩体棕/机器人绿
  3. `src/lib/exportCSV.ts` — CSV传感器数据 (RFC 4180, UTF-8 BOM, 兼容Excel/ERP/SCADA)
     - 4大数据区: 裂缝节点19项传感器矩阵 + 机器人状态 + 告警事件 + 裂缝几何参数
  4. `src/lib/pdfExport.ts` — 已有PDF报告 (整合进Hub)
- **新建UI组件**: `src/components/layout/ExportHub.tsx` — Modal对话框, 4卡片式导出, 实时统计预估点数/大小
- **TopBar改造**: "导出报告"按钮→"数据导出"按钮→打开ExportHub Modal
- **新建标准文档**: `docs/EXPORT_STANDARD.md` — 导出合规标准(LAS/OBJ/CSV格式规范+ASPRS分类码+PR Review检查项)
- **tsc**: 零错误

### 第九轮：产品标准文档统一索引
- **用户需求**: 所有产品标准(色彩/导出/管道/数据格式)必须集中管理，建立索引根文件，不能散落
- **新建**: `docs/README.md` — 产品标准唯一入口索引
  - 三级分类: 强制规范(S1色彩/S2导出/S3 API契约) → 设计参考(R1 UI对标/R2数据格式) → 规格文档(P1 v2架构)
  - 强制规则速查表: 6条规则+代码检查方式
  - 文档管理规则: 新增规范必须登记到索引, 强制规范必须标注级别和代码真源
- **登记**: API_CONTRACT.md (根目录)也被纳入索引

### 第十轮：全量 UX 审计 (3角色视角)
- **用户需求**: 以矿长/测绘工程师/核电站工人视角全量测试每个按钮和交互
- **新建**: `docs/UX_AUDIT_REPORT.md` — 45项问题, 4级分类(C/M/ME/MI)
  - **5项阻断性(CRITICAL)**: C1告警无确认按钮 / C2 AI对话失败卡死 / C3测量工具无操作指引 / C4无移动端适配 / C5无声音告警
  - **10项重大(MAJOR)**: M1面板过载 / M2空状态不适配 / M3指南针不旋转 / M4开发者标签暴露 / M5导出无进度条 / M6无批量导出 / M7无新手引导 / M8无连接测试 / M9字体极小 / M10水印过激
  - **15项中度(MEDIUM)**: 无排序/无节点详情/趋势窗口固定/无全屏等
  - **15项轻度(MINOR)**: 版本号占位/无搜索建议/颜色小不一致等
- **已登记到索引**: docs/README.md → R3

### 第十一轮：UX 审计问题全量修复 + 5场景自测
- **用户需求**: 修复审计发现的全部问题，修复后从5场景对应角色自测，通过后验收
- **修复 16 项** (5 CRITICAL + 10 MAJOR + 1 MEDIUM):
  - **C1**: AlertFeed 每条告警加"确认"按钮 + "全部确认" + store 新增 acknowledgedAlertIds
  - **C2**: ChatPanel handleSend 加 try/catch，失败显示错误气泡而非永久 loading
  - **C3**: MeasurementToolbar 激活工具后浮出具体操作指引（如"依次点击两个点测量距离"）
  - **C4**: App.tsx 加 MobileWarning 组件（md:hidden 小屏引导）
  - **C5**: AlertFeed danger 告警触发时 Web Audio API 播放 880→660Hz 双音
  - **M1**: ControlPanel 标题改为实时状态灯
  - **M2**: FractureDetailPanel 空状态 7 种场景各自文案
  - **M3**: MainLayout 指南针根据相机方位角 atan2(x,z) 实时旋转
  - **M4**: 移除 Scene3DCanvas 底部 Four.js/Potree/Open3D/DeepSeek 标签 + ControlPanel SHADCN/RADIX 标签
  - **M5**: ExportHub 加 ESC 关闭（导出中禁止关闭）
  - **M6**: ExportHub 加"一键全部导出"按钮
  - **M8**: SettingsDialog 加"测试连接"按钮（ping 请求）
  - **M9**: 全局 text-[8px] → text-[9px]（34处批量 sed 替换）
  - **M10**: WatermarkOverlay 移除大字+遮罩，改为右上角小标签
  - **ME14**: ExportHub open 变化时清除 done/progress
- **5 场景自测**: 33 项全部通过
- **修改文件**: store/useSceneStore.ts / AlertFeed.tsx / ChatPanel.tsx / MeasurementToolbar.tsx / ControlPanel.tsx / FractureDetailPanel.tsx / ExportHub.tsx / SettingsDialog.tsx / WatermarkOverlay.tsx / MainLayout.tsx / Scene3DCanvas.tsx / App.tsx / UX_AUDIT_REPORT.md
- **tsc**: 零错误 / **build**: 5.37s 通过

### 第十二轮：AI Prompt 场景化拆分 + 防幻觉约束
- **用户需求**: 每个场景必须发不同的 system prompt（之前煤矿/金矿/油气共用一套），且必须有防幻觉约束
- **修复内容**:
  1. **拆分 1→3**: 原 `SYSTEM_PROMPT_FRACTURE` 拆为 3 套独立 prompt:
     - `SYSTEM_PROMPT_COAL`: 煤矿地质安全工程师（瓦斯突出/CH4/CO/渗透率）
     - `SYSTEM_PROMPT_GOLD`: 岩土力学工程师（岩爆预警/微震/应力集中）
     - `SYSTEM_PROMPT_OIL`: 油气储层工程师（渗透率/孔隙压力/压裂）
  2. **新增防幻觉约束** `ANTI_HALLUCINATION` 常量追加到全部 7 套 prompt:
     - 只用实时数据，不编造数值
     - 数据不足必须说明，不猜测
     - 标准号不确定不编造
     - 安全建议保守
  3. **pipeline/nuclear/refinery/underground 4 套 prompt 加上身份定位**（如"你是核工程管道完整性工程师"）
  4. **getSystemPrompt 更新**: 7 case 各自映射，不再 default 共用
  5. **buildSceneContext 修复**: 所有 Record map 补齐 `underground` 条目（scenarioNames/sensorKey/sensorLabel/thresholdLabelMap/排序/超阈值/auxDescMap），之前缺失会导致运行时 undefined
- **修改文件**: src/api/aiApi.ts / src/api/llmTools.ts
- **tsc**: 零错误 / **build**: 5.84s 通过

### 第十三轮：AI Prompt 通用规则文档化 + 代码架构重构
- **用户需求**: 通用规则必须抽成独立文档，否则新增场景时不可能每次重复约束
- **新建文档**: `docs/AI_PROMPT_STANDARD.md`（S4 强制规范）
  - 三层架构: Layer1 通用能力 + Layer2 场景配置 + Layer3 通用规范+防幻觉
  - 新增场景 Checklist: 只需加 SCENE_PROMPTS 配置项 + buildSceneContext Record map
  - PR Review 检查项
- **代码重构** `src/api/aiApi.ts`:
  - 通用规则抽为 3 个常量: `COMMON_ABILITY`(言出法随) / `COMMON_RULES`(回答规范) / `ANTI_HALLUCINATION`(7条防幻觉)
  - 7 个场景从独立字符串常量改为 `SCENE_PROMPTS` 数据配置表（Record<ScenarioType, ScenePromptConfig>）
  - 每个配置项 5 个字段: name/identity/toolExamples/extraRules/knowledge
  - `getSystemPrompt()` 自动拼装: 身份 + COMMON_ABILITY + 工具示例 + COMMON_RULES + 专属规则 + 知识 + ANTI_HALLUCINATION
  - 新增场景只需加一个配置项，通用规则自动注入
- **文档索引**: docs/README.md 新增 S4 条目 + 2 条 PR Review 规则
- **tsc**: 零错误 / **build**: 6.32s 通过

### 第十四轮：Potree 加载崩溃修复 (proj4 缺失)
- **错误**: WebView 报 `proj4 is not defined` + `Potree 加载失败 — Viewer 未导出（jQuery 可能未正确加载）`
- **根因**: `public/potree/` 目录只有 `jquery.min.js` + `potree.js`，缺少 `proj4.js`。Potree 在初始化时直接引用全局 `proj4` 做坐标投影，找不到就抛 ReferenceError，导致 Viewer 类未定义
- **修复**: 
  1. 下载 proj4js 2.9.0 到 `public/potree/proj4.js`（89KB）
  2. `PotreeViewer.tsx` 的 `loadPotreeScript()` 加载顺序改为 jQuery → proj4 → CSS → potree.js
- **tsc**: 零错误

### 第十六轮：任务回放演示模式（Playback）
- **用户需求**: 竞品展示机器人逐步探测过程，我们只展示最终结果。需要"播放键"让客户理解数据是逐步采集、清洗、渲染的
- **实现方案**:
  1. **Store** (`useSceneStore.ts`): 新增 `playbackProgress`(0~1) / `isPlaying` / `playbackSpeed` 状态 + `startPlayback()`/`stopPlayback()`
  2. **回放引擎** (`PlaybackController.tsx` 新建):
     - `PlaybackEngine` — R3F Canvas 内的 useFrame 驱动器，每帧按 delta/speed 推进进度（默认 15 秒播完）
     - `PlaybackBar` — 浮动控制条：播放/暂停/重播/进度条/速度(0.5x~4x)/完成按钮/空格快捷键
  3. **FractureNetwork.tsx**: 回放时按进度逐步显示裂缝（主干优先），同时按比例截断 path/nodes 模拟"扫描未完成"
  4. **RobotMarkers.tsx**: 回放时只显示已"部署"的机器人（按索引比例过滤）
  5. **Scene3DCanvas.tsx**: Canvas 内挂载 `<PlaybackEngine />`，外部挂载 `<PlaybackBar />`
- **交互**: 空格键播放/暂停；进度条可拖拽跳转；速度 0.5x/1x/2x/4x 可切换
- **tsc**: 零错误 / **build**: 7.52s 通过
- **用户反馈**: 所有场景图层控制里都写"地质岩体"，但石油/炼化/核燃料跟地质无关
- **根因**: `LayerToggle.tsx` 的 `layerConfig` 是硬编码数组，`rockMass` 条目固定写"地质岩体"
- **实际情况**: rockMass 图层在不同场景控制的渲染对象不同:
  - coal/gold → RockMass 地质岩体
  - oil → RockMass 储层岩体
  - pipeline → RockMass 管沟覆土
  - nuclear → ReactorContainment 安全壳厂房（非岩体）
  - refinery → 什么都不渲染（RockMass 和 ReactorContainment 都 return null）
  - underground → RockMass 岩溶围岩
- **修复** (`LayerToggle.tsx`):
  - 新增 `STRUCTURE_LAYER` 场景映射表，按场景返回正确 label/desc/Icon
  - refinery 场景隐藏 rockMass 开关（无渲染对象）
  - nuclear 场景用 Atom 图标 + "安全壳厂房"
  - 其他图层描述也通用化（去掉"蛇形"等场景限定词）
- **tsc**: 零错误

### 第十七轮：任务回放引擎全面重构 — 真实机器人探测模拟
- **用户需求**: 原回放只做了"逐步显示"和"按比例过滤机器人"，不像真实探测。需要模拟200个机器人沿管道爬行、逐步扫描管道、个别卡住/低电量的过程
- **新建 `src/lib/playbackEngine.ts`** — 回放引擎核心:
  - `assignRobotsToFractures()`: 机器人→裂缝路径分配（round-robin 到主干裂缝）+ 确定性伪随机分配速度/故障
  - `computeRevealRatios()`: 主干裂缝先于分支揭示（共享函数，FractureNetwork 和 RobotMarkers 共用）
  - `computePlaybackState()`: 根据进度计算每个机器人沿 CatmullRom 曲线的动画位置 + 每条裂缝的揭示比例
  - 阶段1(0~0.15): 机器人在入口分批部署（错开时间）
  - 阶段2(0.15~1.0): 机器人沿管道爬行（速度 0.7~1.3x）
  - 故障分布（均匀哈希，修正 abs(cos) 密度偏移）: 4% error + 4% offline + 10% low_battery + 82% normal
- **FractureNetwork.tsx**:
  - 所有 Mesh 组件接收 `revealRatio` 参数，按比例截断 path 路径渲染
  - 新增 `PlaybackScanPoints`: instancedMesh 渲散点云（揭示早期稀疏→后期密集），useEffect 替代 useFrame 避免每帧抖动
  - 测点标记在回放时隐藏（性能）
- **RobotMarkers.tsx**: 回放时用 `computePlaybackState` 的动画位置替代静态位置
- **状态机修正**: reveal/robot 判断从 `isPlaying` 改为 `playbackProgress < 1`，修复暂停/拖滑块时场景闪烁
- **物理一致性**: 机器人 pathT 被 clamp 到 revealRatio（不能跑到未探测管道外）
- **性能修复**: O(n²) indexOf → O(n) 索引遍历；缓存 CatmullRomCurve3 和 assignments
- **UI**: PlaybackBar 位置从 bottom-16 改为 bottom-2（紧贴 AI 助手标题上方，不遮挡画面）
- **数学验证**: Node.js 测试脚本验证 reveal 单调性、main≥branch 排序、robot-reveal 同步、部署分批、故障分布
- **tsc**: 零错误 / **build**: 5.64s 通过

### 第十八轮：修复回放分支裂缝提前渲染（违反物理逻辑）
- **用户反馈**: 回放演示中分支裂缝在机器人还没到达汇合点时就渲染出来了，太离谱
- **根因**: `computeRevealRatios()` 中分支裂缝按纯时间表揭示（progress 0.3+ 开始），完全不考虑母体裂缝的探测是否已经到达分支入口。机器人只被分配到主裂缝，分支的 `revealMax` 恒为 0，所以分支揭示纯粹由时间驱动
- **修复** (`src/lib/playbackEngine.ts`):
  - 新增 `computeBranchJunction()`: 对每条分支，通过空间最近匹配找到其入口(path[0])在母体路径上的位置（junctionFraction 0~1）。优先使用 `parentFractureId`，为 null（如核反应堆管网）时搜索所有主裂缝
  - 新增 `_junctionCache` 缓存计算结果
  - 重写 `computeRevealRatios()` 的分支逻辑: `parentReveal < junctionFraction` 时分支完全不显示；之后按越过汇合点的母体探测量映射为分支揭示比例，加 stagger 错开但保证最终到 1.0
  - `resetPlaybackCache()` 增加 `_junctionCache` 清除
- **数学验证**: 0 违规（无分支在母体到达汇合点前揭示）；所有分支在 progress 0.7 前达到 1.0
- **tsc**: 零错误 / **build**: 5.19s 通过

### 第十九轮：回放三大问题修复（分支无机器人 + 最终弹出 + 速度选项）
- **用户反馈**: (1) F7 分支裂缝渲染了但没机器人爬过去 (2) 回放结束突然弹出一堆机器人 (3) 速度太快，需要 1x/10x/50x
- **根因分析**:
  1. `assignRobotsToFractures()` 只把机器人分配到主裂缝（line 117-118），分支零机器人
  2. `computePlaybackState()` 在 `progress >= 1` 时直接返回全部静态机器人 → 视觉弹出
  3. 基础时长 20 秒太快，速度选项 [0.5,1,2,4] 不合理
- **修复** (5 文件):
  1. **playbackEngine.ts**: 加权分配到所有裂缝（主干×3, 其他×2, 分支×1）；移除 progress>=1 早返回，改用最后 10% ramp 让所有机器人平滑汇聚到终点
  2. **useSceneStore.ts**: 新增 `playbackActive` 标志 — startPlayback 设 true，stopPlayback（用户点"完成"）设 false；播放自然结束时保持 true（无弹出）
  3. **FractureNetwork.tsx + RobotMarkers.tsx**: 判断条件从 `playbackProgress >= 1` 改为 `playbackActive`
  4. **PlaybackController.tsx**: 基础时长 20→1000 秒（1x=1000s/10x=100s/50x=20s）；速度按钮 [0.5,1,2,4]→[1,10,50]；默认速度 50x
- **验证**: 200 机器人分配 → 主干各 55 个，分支各 18 个（分支有机器人 ✅）；tsc 零错误；build 5.25s 通过

### 第二十轮：根本架构修复 — 管道揭示从"时间驱动"翻转为"机器人驱动"
- **用户质疑**: "分支裂缝没有机器人，你怎么渲染出来的？你怎么探测到的？说明之前的就不靠谱"
- **根因**: `computeRevealRatios()` 是纯时间驱动的，`computePlaybackState()` 用它来夹住机器人位置（`pathT = Math.min(pathT, revealRatio)`）。依赖方向反了——管道先按时间揭示，机器人再跟着走。导致管道揭示跟机器人位置完全脱钩
- **修复** (`src/lib/playbackEngine.ts` + `src/components/scene/FractureNetwork.tsx`):
  1. 重写 `computePlaybackState()`: 三步走 — 先算主裂缝机器人位置（时间×速度），再算分支机器人位置（由母体最远机器人物理越过汇合点门控），最后揭示比例 = 每条裂缝最远机器人的 pathT
  2. `FractureNetwork.tsx` 改为调用 `computePlaybackState(allRobots, fractures, progress).revealRatios`，与 RobotMarkers 完全共享同一真相源
  3. 移除了 `computeRevealRatios` 的调用点（函数仍保留但已无引用）
- **关键变化**: 不再是"时间→管道→机器人"，而是"时间→机器人→管道"。机器人爬到哪，管道才揭示到哪
- **验证**: 0 违规 — B0 (junction@0.263) 在 M0 机器人到 0.34 时才出现；B1 (junction@0.789) 在 M1 到 0.88 时才出现；tsc 零错误；build 5.12s 通过

### 第二十一轮：修复炼油化工场景分支无机器人分配
- **用户反馈**: 检查所有五个模拟的分支管线，有没有分配机器人，渲染出来但没探测是假的
- **全面检查 5 个场景**:
  1. 地下裂缝 (coal): 18 裂缝 (6主+12分支), 200 机器人 — ✓ 正常
  2. 管线 (pipeline): 22 裂缝 (10主+12分支), 150 机器人 — ✓ 正常
  3. 核反应堆 (nuclear): 49 裂缝 (23主+26分支), 180 机器人 — ✓ 正常
  4. **炼油化工 (refinery): 78 裂缝 (43主+35分支), 160 机器人 — ✗ 4条分支0个机器人**
  5. 地下暗流 (underground): 21 裂缝 (3主+18分支), 160 机器人 — ✓ 正常
- **根因**: round-robin 加权列表 `i % finalTargets.length` 在裂缝数>机器人数时，排在末尾的分支 slot 永远不会被访问到（炼油化工加权后 210 slots，只有 160 个机器人）
- **修复** (`src/lib/playbackEngine.ts` `assignRobotsToFractures()`): 用加权比例分配替代 round-robin — 先保证每条裂缝至少 2 个机器人(MIN_PER_FRACTURE)，剩余按权重比例(main×3, other×2, branch×1)分配
- **验证**: 全部 5 场景 ✓，分支最少机器人数分别为 7/4/2/2/6；tsc 零错误；build 5.16s 通过

### 第二十二轮：三个严重一致性修复
- **用户反馈**: ①机器人数必须跟数据点一致 ②状态/颜色是否统一按规则渲染 ③PotreeViewer 报错
- **问题1 — 机器人数不一致**: 回放时 undeployed 机器人被 `continue` 跳过，progress=0.1 时场景只显示 ~66% 机器人，但控制面板说 200 个
  - **修复**: 未部署/母体未到汇合点的机器人设 pathT=0（留在入口）而不是跳过 → 全部 N 个机器人在任意 progress 都在场景中
- **问题2 — 状态/颜色不统一**: `assignRobotsToFractures` 用独立随机数生成 stallType（4% error/4% offline/10% low_battery），跟数据里的 `robot.status` 完全脱钩 → 回放中 online 机器人可能变红(error)，颜色不一致
  - **修复**: 从 `robot.status` 直接派生停机行为 — error→卡在15-50%, offline→卡在20-60%, low_battery→半速, maintenance→留入口, online→全速。status/meshConnected/task 全部直接用数据原始值
- **问题3 — PotreeViewer 崩溃**: ①`viewer.setNavigationMode is not a function` ②`BinaryHeap is not defined`
  - **修复1**: `setNavigationMode` 包 try-catch
  - **修复2**: 在 potree.js 加载前注入 BinaryHeap 类（min-heap 实现），因为 Potree 1.8 引用但未定义
- **验证**: 全部 5 场景 × 9 个 progress 点 = 45 次检查，0 错误 — 机器人数始终=数据数, 状态分布完全匹配；tsc 零错误；build 5.44s 通过

### 第二十三轮：回放开始时机器人从地表入口出发（不再凭空出现在地下）
- **用户反馈**: 地下暗流场景点播放后，章鱼机器人已经散落在地下岩层中，没有从入口爬进去
- **根因**: 分支裂缝的 `path[0]`（入口）在地下深处（y=-26~-30）。pathT=0 的机器人被放在各自裂缝的 `path[0]` → 一开始就散在地下
- **所有 5 场景的分支入口对比**:
  - 地下裂缝: 主入口 y=18~20（地表），分支入口 y=-19~-9（地下）
  - 管线: 主入口 y=19（地表），分支入口 y=-6~-3（地下）
  - 核反应堆: 主入口 y=4（高处），分支入口 y=-5~0（设备内部）
  - 炼油化工: 主入口 y=35（塔顶），分支入口 y=-10~-7.5（地下）
  - 地下暗流: 主入口 y=-3（地表），分支入口 y=-30~-23（地下深处）
- **修复** (`src/lib/playbackEngine.ts`): 新增 `getRootEntryPoint()` — 追溯每条裂缝的根主裂缝地表入口
  - 主裂缝: 用自己的 path[0]；若主裂缝自身在地下（如深层连通暗河UR3 y=-23）则取最近地表主裂缝入口
  - 分支: 沿母体关系（computeBranchJunction）追溯到根主裂缝入口
  - pathT=0 的机器人聚集在地表入口，随 pathT 增大平滑过渡到裂缝入口
- **验证**: 5 场景 progress=0 时 0 散落（全部机器人距最近地表入口 <10m）；50% 进度时机器人已爬行到深处；tsc 零错误；build 14.69s 通过

### 第二十四轮：机器人不再直线飞行 + 倍速选项
- **用户反馈**: 机器人从入口飘到某处，接力探索到终端后直接直线飞过去继续接力；要求 20x/30x 倍速
- **根因**: 分支机器人在 `pathT < 0.05` 时从**地表入口直线插值**到裂缝入口（地下深处），跨距可达百米，视觉上就是从空中直线飞过去
- **修复** (`src/lib/playbackEngine.ts` Step 3): 分支机器人在 `pathT < 0.03` 时**沿母体裂缝曲线爬行**到汇合点（跟随母体队伍），然后平滑过渡到分支曲线 — 始终沿管道/裂缝路径移动
- **倍速**: PlaybackController 速度选项从 `[1, 10, 50]` 改为 `[1, 10, 20, 30, 50]`
- **验证**: 5 场景 × 8 进度 = 40 次检查，0 飞行机器人，最大偏离 2.1m（仅入口散布）；build 8.76s 通过

### 第二十五轮：全面修复5场景路径问题 + 扫描点闪烁
- **用户反馈**: ①闪烁黄光点是什么 ②地下暗流从岩层中间开始 ③要求全面检查不要一个一个发现
- **全面审计发现的问题**:
  1. **深层主裂缝** (管线4条/核反应堆19条/炼油化工40条/暗流1条): `type='main'` 但 `path[0]` 在地下深处
  2. **computeBranchJunction 自匹配**: 搜索候选母体时包含自身 → 深层主裂缝匹配到自己 path[0]
  3. **过渡区直线飞行**: 母体连接点和裂缝入口距离>3m时直线插值穿过空隙
  4. **扫描点闪烁**: `PlaybackScanPoints` 用 `Math.random()` 每帧重新随机散点
- **修复**:
  1. `computeBranchJunction`: 排除自身 + 对深层主裂缝优先搜索地表主裂缝作为母体
  2. Step 1: 深层主裂缝（非地表入口）需母体到达连接点后才开始爬行，否则 pathT=0
  3. Step 3: `needsParentApproach` 条件扩展到深层主裂缝；母体连接点与裂缝入口距离>3m时留在母体曲线上不做直线过渡
  4. `PlaybackScanPoints`: 确定性伪随机 (`seed * 16807 % 2147483647`) 替代 `Math.random()`
- **验证**: 5 场景 × 11 进度点 = 55 次检查，0 问题（飞行/深层出现/曲线偏离均通过）；tsc 零错误；build 8.22s 通过

### 第二十六轮：入口标记环过滤 — 只有真实可部署入口才显示
- **用户反馈**: 场景里各种圈环太多太乱，地下暗流的入口环在岩层深处，机器人根本放不进去
- **问题**: `FractureNetwork.tsx` 对所有 `type==='main'` 的裂缝/管道都在 `path[0]` 放入口环，包括深层连通管道（如暗流 Trunk3 起点在 y≈-17）
- **修复**: 计算所有主管道起点的最高Y(surfaceY)和Y跨度(ySpan)，阈值=max(5, ySpan×0.25)，只有 `path[0].y >= surfaceY - 阈值` 的主管道才显示入口环
- **效果**: 暗流保留2个地表入口(Trunk1/2)，移除深层连通(Trunk3)；管线保留6个井口立管(y=19)，移除地下汇集管(y=-6)；核反应堆只保留最高处主蒸汽管；炼油化工只保留塔顶管道
- **tsc**: 零错误 / **build**: 12.73s 通过
